const initial = {
    isLogin:false,
    baseInfo:null
  }
  export const userReducer = (state = initial,action) => {
    switch (action.type) {
      default:
        return state
    }
  }
  
  export default userReducer
  